const clock=document.getElementById('clock'),connectBtn=document.getElementById('connectBtn'),simulateBtn=document.getElementById('simulateBtn'),clearBtn=document.getElementById('clearBtn'),connectionText=document.getElementById('connectionText'),modeLabel=document.getElementById('modeLabel'),exposureCard=document.getElementById('exposureCard'),exposureTitle=document.getElementById('exposureTitle'),signal=document.getElementById('signal'),responseRing=document.getElementById('responseRing'),h2sRingValue=document.getElementById('h2sRingValue'),responseText=document.getElementById('responseText'),responseDesc=document.getElementById('responseDesc'),eventCount=document.getElementById('eventCount'),statusNote=document.getElementById('statusNote'),systemStatus=document.getElementById('systemStatus'),history=document.getElementById('history'),lastSync=document.getElementById('lastSync'),sampleIdEl=document.getElementById('sampleId'),sampleTime=document.getElementById('sampleTime'),rawSensorValue=document.getElementById('rawSensorValue'),workerId=document.getElementById('workerId'),workerIdTitle=document.getElementById('workerIdTitle'),healthWorker=document.getElementById('healthWorker'),wristbandId=document.getElementById('wristbandId'),deviceBadge=document.getElementById('deviceBadge'),activeWorkers=document.getElementById('activeWorkers'),attendanceState=document.getElementById('attendanceState'),attendanceValue=document.getElementById('attendanceValue'),healthAttendance=document.getElementById('healthAttendance'),entryTimeEl=document.getElementById('entryTime'),exitTimeEl=document.getElementById('exitTime'),durationValue=document.getElementById('durationValue'),durationStat=document.getElementById('durationStat'),healthDuration=document.getElementById('healthDuration'),shiftBadge=document.getElementById('shiftBadge'),attendanceHistory=document.getElementById('attendanceHistory'),h2sValue=document.getElementById('h2sValue'),h2sStatus=document.getElementById('h2sStatus'),h2sNote=document.getElementById('h2sNote');
const SERVICE_UUID='7b2a0001-6c4a-4d3b-9c1f-4153454e544c',CHARACTERISTIC_UUID='7b2a0002-6c4a-4d3b-9c1f-4153454e544c',DB_KEY='airSentinalSamplesV1',ATTENDANCE_KEY='airSentinalAttendanceV1',SHIFT_KEY='airSentinalCurrentShiftV1';
let characteristic=null,events=loadEvents(),attendanceRecords=loadAttendance(),shift=loadShift();
function now(){return new Date().toLocaleTimeString([],{hour:'2-digit',minute:'2-digit',second:'2-digit'})}function updateClock(){clock.textContent=now();updateDuration()}setInterval(updateClock,1000);updateClock();
function loadEvents(){try{return JSON.parse(localStorage.getItem(DB_KEY)||'[]')}catch{return[]}}function saveEvents(){localStorage.setItem(DB_KEY,JSON.stringify(events.slice(0,100)))}function loadAttendance(){try{return JSON.parse(localStorage.getItem(ATTENDANCE_KEY)||'[]')}catch{return[]}}function saveAttendance(){localStorage.setItem(ATTENDANCE_KEY,JSON.stringify(attendanceRecords.slice(0,100)))}function loadShift(){try{return JSON.parse(localStorage.getItem(SHIFT_KEY)||'null')}catch{return null}}function saveShift(){if(shift)localStorage.setItem(SHIFT_KEY,JSON.stringify(shift));else localStorage.removeItem(SHIFT_KEY)}
function formatDuration(ms){if(!Number.isFinite(ms)||ms<0)ms=0;const s=Math.floor(ms/1000),h=Math.floor(s/3600),m=Math.floor(s%3600/60),sec=s%60;return[h,m,sec].map(v=>String(v).padStart(2,'0')).join(':')}function formatDateTime(iso){return iso?new Date(iso).toLocaleString([],{year:'numeric',month:'short',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit'}):'Not recorded'}function formatTime(iso){return iso?new Date(iso).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit',second:'2-digit'}):'Not recorded'}
function updateDuration(){if(!shift){durationValue.textContent='00:00:00';durationStat.textContent='00:00:00';healthDuration.textContent='00:00:00';return}const end=shift.exit?new Date(shift.exit).getTime():Date.now(),duration=formatDuration(end-new Date(shift.entry).getTime());durationValue.textContent=duration;durationStat.textContent=duration;healthDuration.textContent=duration}
function updateShiftUI(){if(!shift){entryTimeEl.textContent='Not recorded';exitTimeEl.textContent='Not recorded';shiftBadge.textContent='NO ENTRY';shiftBadge.className='badge demo';attendanceValue.textContent='ABSENT';attendanceValue.className='warn-text';healthAttendance.textContent='ABSENT';healthAttendance.className='warn-text';attendanceState.textContent='No active entry';activeWorkers.textContent='00'}else if(!shift.exit){entryTimeEl.textContent=formatDateTime(shift.entry);exitTimeEl.textContent='Not recorded';shiftBadge.textContent='IN PROGRESS';shiftBadge.className='badge connected';attendanceValue.textContent='PRESENT';attendanceValue.className='good';healthAttendance.textContent='PRESENT';healthAttendance.className='good';attendanceState.textContent='Worker is inside';activeWorkers.textContent='01'}else{entryTimeEl.textContent=formatDateTime(shift.entry);exitTimeEl.textContent=formatDateTime(shift.exit);shiftBadge.textContent='SHIFT COMPLETE';shiftBadge.className='badge demo';attendanceValue.textContent='EXITED';attendanceValue.className='';healthAttendance.textContent='EXITED';healthAttendance.className='';attendanceState.textContent='Worker has exited';activeWorkers.textContent='00'}updateDuration()}
function recordEntry(source='MANUAL'){if(shift&&!shift.exit){connectionText.textContent='Worker already has an active entry. Record exit before starting another shift.';return}shift={worker_id:workerId.textContent,wristband_id:wristbandId.textContent,entry:new Date().toISOString(),exit:null,source};saveShift();updateShiftUI();connectionText.textContent=`Entry recorded for ${shift.worker_id}. Duration is now running.`}function recordExit(source='MANUAL'){if(!shift||shift.exit){connectionText.textContent='No active worker entry is available to record an exit.';return}shift.exit=new Date().toISOString();const duration=formatDuration(new Date(shift.exit).getTime()-new Date(shift.entry).getTime());attendanceRecords.unshift({worker_id:shift.worker_id,wristband_id:shift.wristband_id,entry:shift.entry,exit:shift.exit,duration,source});saveAttendance();saveShift();updateShiftUI();renderAttendanceHistory();connectionText.textContent=`Exit recorded for ${shift.worker_id}. Total duration: ${duration}.`}function resetShift(){shift=null;saveShift();updateShiftUI();connectionText.textContent='Current entry/exit session reset. Ready for the next worker entry.'}
document.getElementById('entryBtn').addEventListener('click',()=>recordEntry());document.getElementById('exitBtn').addEventListener('click',()=>recordExit());document.getElementById('resetShiftBtn').addEventListener('click',resetShift);
function h2sStatusFor(ppm){if(!Number.isFinite(ppm))return{label:'NO CALIBRATED READING',className:'none',note:'No calibrated ppm value supplied'};if(ppm<=1)return{label:'LOW',className:'low',note:'0–1 ppm · GREEN'};if(ppm<=5)return{label:'CAUTION',className:'elevated',note:'Above 1–5 ppm · YELLOW'};if(ppm<=10)return{label:'HIGH',className:'high',note:'Above 5–10 ppm · ORANGE'};return{label:'DANGER',className:'over',note:'Above 10 ppm · RED'}}
function randomSample(){const response=Math.floor(18+Math.random()*78),raw=Math.round(response*4095/100),h2s_ppm=Number((Math.random()*12).toFixed(2)),c=h2sStatusFor(h2s_ppm);return{event_id:`AS-${Date.now().toString().slice(-6)}`,worker_id:workerId.textContent||'AS-024',wristband_id:wristbandId.textContent||'WB-024',response_index:response,sensor_raw:raw,h2s_ppm,status:response>=70?'ALERT':response>=40?'UNKNOWN':'SAFE',source:'SIMULATED',timestamp:new Date().toISOString(),concentration_status:c.label}}
function normaliseSample(data){const response=Math.max(0,Math.min(100,Number(data.response_index??data.response??0))),status=String(data.status||(response>=70?'ALERT':response>=40?'UNKNOWN':'SAFE')).toUpperCase(),raw=Number.isFinite(Number(data.sensor_raw))?Number(data.sensor_raw):Math.round(response*4095/100),ppmRaw=data.h2s_ppm??data.h2s_ppm_value??data.concentration_ppm,ppm=ppmRaw===null||ppmRaw===undefined||ppmRaw===''?null:Number(ppmRaw);return{event_id:data.event_id||`AS-${Date.now().toString().slice(-6)}`,worker_id:data.worker_id||'AS-024',wristband_id:data.wristband_id||'WB-024',response_index:Math.round(response),sensor_raw:raw,h2s_ppm:Number.isFinite(ppm)?Math.max(0,ppm):null,status:['SAFE','ALERT','UNKNOWN'].includes(status)?status:'UNKNOWN',source:data.source||'WRISTBAND',timestamp:data.timestamp||new Date().toISOString()}}
function updateH2SDisplay(ppm){const r=h2sStatusFor(ppm);h2sValue.textContent=Number.isFinite(ppm)?ppm.toFixed(2):'—';h2sRingValue.textContent=Number.isFinite(ppm)?ppm.toFixed(2):'—';h2sStatus.textContent=r.label;h2sStatus.className=`h2s-status ${r.className}`;h2sNote.textContent=r.note;responseRing.className=`response-ring ${r.className}`}
function applySample(raw){const sample=normaliseSample(raw),time=formatTime(sample.timestamp),response=sample.response_index,c=h2sStatusFor(sample.h2s_ppm);workerId.textContent=sample.worker_id;workerIdTitle.textContent=sample.worker_id;healthWorker.textContent=sample.worker_id;wristbandId.textContent=sample.wristband_id;sampleIdEl.textContent=sample.event_id;sampleTime.textContent=time;if(rawSensorValue)rawSensorValue.textContent=String(sample.sensor_raw);lastSync.textContent='Just now';updateH2SDisplay(sample.h2s_ppm);const displayStatus=Number.isFinite(sample.h2s_ppm)?c.label:sample.status;exposureCard.classList.toggle('alert-mode',displayStatus==='DANGER'||sample.status==='ALERT');signal.className=`signal ${Number.isFinite(sample.h2s_ppm)?c.className:sample.status.toLowerCase()}`;signal.textContent=displayStatus;
if(Number.isFinite(sample.h2s_ppm)){responseText.textContent=c.label;responseText.style.color='';responseDesc.textContent=`Approx. H₂S concentration: ${sample.h2s_ppm.toFixed(2)} ppm. Color band: ${c.className==='low'?'GREEN':c.className==='elevated'?'YELLOW':c.className==='high'?'ORANGE':'RED'}.`;systemStatus.textContent=(c.className==='over'?'DANGER':c.className==='high'?'HIGH':c.className==='elevated'?'CAUTION':'SAFE');systemStatus.className=c.className==='over'?'bad':c.className==='high'?'warn':c.className==='elevated'?'warn':'good';statusNote.textContent=`H₂S ${sample.h2s_ppm.toFixed(2)} ppm · ${c.label}`}else if(sample.status==='ALERT'){exposureTitle.textContent='Exposure Response Detected';responseText.textContent='HIGH SENSOR RESPONSE';responseText.style.color='#ff8585';responseDesc.textContent=`Sensor response ${response}/100 from raw ADC signal ${sample.sensor_raw}/4095. H₂S ppm requires calibration.`;systemStatus.textContent='ALERT';systemStatus.className='bad';statusNote.textContent='Active sensor-response alert';statusNote.style.color='#ff8585'}else if(sample.status==='UNKNOWN'){exposureTitle.textContent='Uncertain Sensor Response';responseText.textContent='REQUIRES VALIDATION';responseText.style.color='#d6bd6a';responseDesc.textContent=`Sensor response ${response}/100 from raw ADC signal ${sample.sensor_raw}/4095. Ppm interpretation requires experimental calibration.`;systemStatus.textContent='UNKNOWN';systemStatus.className='warn';statusNote.textContent='Calibration / validation required';statusNote.style.color='#d6bd6a'}else{exposureTitle.textContent='Normal Sensor Response';responseText.textContent='NORMAL';responseText.style.color='';responseDesc.textContent=`Sensor response ${response}/100 from raw ADC signal ${sample.sensor_raw}/4095. H₂S concentration is shown separately when calibrated data is available.`;systemStatus.textContent='SAFE';systemStatus.className='good';statusNote.textContent='No active sensor-response alert';statusNote.style.color=''}
if(Number.isFinite(sample.h2s_ppm))exposureTitle.textContent='H₂S Concentration Status';events.unshift(sample);events=events.slice(0,100);saveEvents();eventCount.textContent=String(events.filter(e=>e.status==='ALERT').length).padStart(2,'0');renderHistory()}
function renderHistory(){if(!events.length){history.innerHTML='<tr class="empty"><td colspan="7">No samples recorded yet.</td></tr>';return}history.innerHTML=events.slice(0,20).map(e=>`<tr><td>${formatTime(e.timestamp)}</td><td>${e.event_id}</td><td>${e.worker_id}</td><td>${e.response_index} / 100</td><td>${Number.isFinite(Number(e.h2s_ppm))?Number(e.h2s_ppm).toFixed(2):'—'}</td><td><span class="status-pill ${e.status.toLowerCase()}">${e.status}</span></td><td>${e.source}</td></tr>`).join('')}
function renderAttendanceHistory(){if(!attendanceRecords.length){attendanceHistory.innerHTML='<tr class="empty"><td colspan="6">No completed attendance records yet.</td></tr>';return}attendanceHistory.innerHTML=attendanceRecords.slice(0,20).map(r=>`<tr><td>${r.worker_id}</td><td>${r.wristband_id}</td><td>${formatDateTime(r.entry)}</td><td>${formatDateTime(r.exit)}</td><td>${r.duration}</td><td><span class="status-pill safe">${r.source||'RECORDED'}</span></td></tr>`).join('')}
function clearAlert(){applySample({event_id:'AS-CLEAR',worker_id:workerId.textContent,wristband_id:wristbandId.textContent,response_index:12,sensor_raw:491,h2s_ppm:null,status:'SAFE',source:'MANUAL CLEAR',timestamp:new Date().toISOString()})}simulateBtn.addEventListener('click',()=>applySample(randomSample()));clearBtn.addEventListener('click',clearAlert);
async function connectWristband(){if(!navigator.bluetooth){connectionText.textContent='Web Bluetooth is not available in this browser. Use Chrome or Edge on HTTPS, or use simulation mode.';return}try{connectionText.textContent='Select your AIR SENTINAL wristband from the Bluetooth picker.';const device=await navigator.bluetooth.requestDevice({filters:[{services:[SERVICE_UUID]},{namePrefix:'AIR-SENTINEL'}],optionalServices:[SERVICE_UUID]});device.addEventListener('gattserverdisconnected',()=>{modeLabel.textContent='DEMO MODE';deviceBadge.textContent='DISCONNECTED';deviceBadge.className='badge demo';connectionText.textContent='Wristband disconnected. Simulation remains available.'});const server=await device.gatt.connect();const service=await server.getPrimaryService(SERVICE_UUID);characteristic=await service.getCharacteristic(CHARACTERISTIC_UUID);await characteristic.startNotifications();characteristic.addEventListener('characteristicvaluechanged',handleBluetoothData);modeLabel.textContent='BLUETOOTH LIVE';deviceBadge.textContent='CONNECTED';deviceBadge.className='badge connected';connectionText.textContent=`${device.name||'AIR SENTINAL wristband'} connected. Waiting for sensor samples.`}catch(error){connectionText.textContent=`Connection cancelled or failed: ${error.message}`}}
function handleBluetoothData(event){try{const data=JSON.parse(new TextDecoder().decode(event.target.value));if(data.event_type==='ENTRY'){recordEntry('WRISTBAND');return}if(data.event_type==='EXIT'){recordExit('WRISTBAND');return}applySample(data)}catch(error){connectionText.textContent='Received data was not valid JSON. Check the wristband firmware format.'}}connectBtn.addEventListener('click',connectWristband);
renderHistory();renderAttendanceHistory();updateShiftUI();updateH2SDisplay(null);eventCount.textContent=String(events.filter(e=>e.status==='ALERT').length).padStart(2,'0');
document.querySelector('.alert-dismiss')?.addEventListener('click',()=>document.getElementById('uxAlert')?.remove());
document.querySelectorAll('.nav-item').forEach(link=>link.addEventListener('click',()=>document.querySelectorAll('.nav-item').forEach(x=>x.classList.toggle('active',x===link))));
